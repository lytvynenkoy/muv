# MUV — React Js + Vite

Minimal setup to build a landing page with React, Vite (SWC), and feature-first structure.

## Requirements

- Node `20` (see `.nvmrc`)
- npm `10+`

## Run

```bash
npm ci
npm run dev
# open http://localhost:5173
```
